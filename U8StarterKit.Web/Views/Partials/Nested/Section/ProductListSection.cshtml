@inherits Umbraco.Web.Mvc.UmbracoViewPage<ProductListSection>
@using U8StarterKit.Web.Models.Umbraco

@foreach (var productNode in Model.Products)
{
    var product = new Product(productNode);
    @Html.Partial("~/Views/Partials/Elements/ProductElement.cshtml", product)
}
