@inherits Umbraco.Web.Mvc.UmbracoViewPage<FooterLinkSettings>
@using U8StarterKit.Web.Models.Umbraco
@using U8StarterKit.Web.Extensions

@if (Model.FooterLink.HasValue())
{
    <div class="columns is-centered">
        <div class="column is-one-third has-text-centered has-spacing-top has-no-background">
            <div class="buttons are-large is-centered">
                <a href="@Model.FooterLink.Url" target="@Model.FooterLink.Target" class="button is-link">@Model.FooterLink.Name</a>
            </div>
        </div>
    </div>
}